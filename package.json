{
  "name": "seeiendom",
  "description": "Get data from seeiendom.no",
  "version": "5.0.1",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "index.js",
  "engines": {
    "node": ">=8.11.1"
  },
  "scripts": {
    "test": "standard && nsp check && tap --reporter=spec test/**/*.js",
    "coverage": "tap test/**/*.js --coverage",
    "coveralls": "tap --cov --coverage-report=lcov test/**/*.js && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [
    "seeiendom",
    "matrikkelen",
    "API"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zrrrzzt/seeiendom.git"
  },
  "bugs": {
    "url": "https://github.com/zrrrzzt/seeiendom/issues"
  },
  "homepage": "https://github.com/zrrrzzt/seeiendom#readme",
  "standard": {
    "rules": {
      "no-eval": false
    }
  },
  "devDependencies": {
    "coveralls": "3.0.1",
    "nsp": "3.2.1",
    "standard": "11.0.1",
    "tap": "11.1.5"
  },
  "dependencies": {
    "iconv-lite": "0.4.23"
  }
}
